interface:
  display_name: "Delegated Engineering"
  short_description: "Orchestrate repo work with minimal safe delegation"
  default_prompt: "Use $delegated-engineering to orchestrate this repository task with the smallest safe delegated workflow."

policy:
  allow_implicit_invocation: true
