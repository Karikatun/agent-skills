# Sample Runner

A local job runner.

Jobs always publish their result. Publication errors fail the job. The default timeout is 60 seconds. Coverage below 80 percent is rejected by the backend.

## Unrelated note

The report title is configurable.
